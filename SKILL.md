---
name: arknights-medal-designer
description: 将用户上传的照片自动制作成《明日方舟》或《明日方舟：终末地》风格的蚀刻章图片。触发场景：用户提到「蚀刻章」「照片变蚀刻章」「明日方舟蚀刻章生成」「终末地蚀刻章制作」，或上传照片并希望转成游戏风格金属蚀刻章。自动完成主体提取、XDoG 蚀刻风格化与章体合成，全程本地处理。
---

# 方舟蚀刻章设计器 · Arknights Medal Designer

把**任意图片**（人像、武器/器物图标、风景照、几何徽标……）变成《明日方舟》官方风格蚀刻章或《终末地》阳极氧化金属风蚀刻章 PNG。
引擎具备输入感知与抽象化能力：自动识别「带透明通道图形 / 平面图形 / 照片」，照片先做纹理压平与分面简化再蚀刻化——不专精某一类题材。
设计语言参照 PRTS 公开官方素材归纳：尖角正六边形；方舟=多层同心细线套环+哑光网点底+嵌入式横字带，终末地=单细深描边+顶部挂扣+阳极氧化金属反光。

## 能力与流程

1. **输入感知**：检测透明通道与内容类型；带 alpha 的设计资产直接使用并保持锐利，照片走抠图 + 纹理压平（中值滤波+细节回注）。
2. **主体提取**：边界泛洪 + 背景主色屏障 + 多部件保留 + 孔洞填充；低置信自动回退整图并以圆角羽化融入章面。
3. **蚀刻纹章化**三种模式：
   - `line` 蚀刻线稿（默认）：|DoG| 轮廓 + 暗部斜排线
   - `silhouette` 剪影纹章：按色板极性的整块实心 + 轮廓光
   - `facet` 分面平涂：直方图均衡四档硬分层（适合风景/大色块场景）
4. **章体合成**：
   - `arknights`：`silver` 普通款 / `plated` 镀层（整面全息镀膜，骨骼不变）/ `gold` 活动金章
   - `endfield`：正六边形阳极氧化金属蚀刻章，品阶 `silver`(银色初期) → `gold`(金色加工) → `iridescent`(特殊炫彩镀层)；章面文字极简

## 如何调用

引擎脚本位于本 skill 目录下 `engine/badge_engine.py`，仅依赖 Python 3.10+ 与 numpy、Pillow（本机已具备）。

```powershell
$env:PYTHONIOENCODING='utf-8'
python "<skill目录>/engine/badge_engine.py" "<输入照片路径>" -o "<输出.png>" --style arknights --text "名字" --subtitle "副标" --serial "编号"
```

参数说明：
- `--style`：`arknights`（默认，官方蚀刻章语言）| `endfield`（终末地阳极氧化金属蚀刻章）
- `--tone`：
  - 方舟：`silver`(普通) | `plated`(镀层全息) | `gold`(活动金章)
  - 终末地：`silver`(银色初期) | `gold`(金色加工) | `iridescent`(特殊炫彩镀层)
- `--text`：主铭文（方舟横幅字带用，建议 ≤8 字）；`--subtitle`：顶部标签；`--serial`：编号注释
- `--number`：数字层级（如 100；终末地为角部数字 label）
- `--line-strength`：0.4~1.8 线稿强度（默认 1.0）
- `--detail`：0.5~2.0 细节密度（默认 1.0）
- `--matting-tol`：抠图容差（默认 26；背景杂乱时可降到 18~22）
- `--no-matting`：跳过抠图整图入章
- `--mode`：`emblem` AI设计纹章（主路径，需 --emblem-design）| `line` 蚀刻线稿 | `icon` 图标化兜底 | `silhouette` 剪影 | `facet` 分面
- `--emblem-design`：AI 纹章设计稿 JSON 文件路径

输出固定写进 `-o` 指定路径；不指定时写入项目 `output/` 目录。命令 stdout 会打印最终文件路径，stderr 可能出现 `[warn] 自动抠图置信度低` 提示（此时结果为整图入章，应主动告知用户）。

## AI 设计纹章工作流（主路径，必读）

真实蚀刻章是"把对象抽象成符号化图案"，不是照片滤镜。默认流程：

1. **看输入图并设计纹章**（视觉能力自适应：若你自带图像识别，直接 read_image；
   否则委派任意可用的视觉子模型执行，不依赖特定模型名）：
   - 完整的**设计提示词模板、图元格式规范、质检清单**见本目录 `PROMPTS.md` §二/§三
   - 核心规则：转译不临摹、对称构图、三档明度 0.22/0.55/0.88、游戏相关输入必须做游戏关联分析
2. 设计稿存为临时 JSON 文件，调用引擎：
   `python engine/badge_engine.py <原图> --mode emblem --emblem-design <json路径> --style arknights --tone silver --text ...`
3. 出图后按 PROMPTS.md §三 的质检清单逐项自检（含图层/顶点/字体/冲突/质感五大类），
   不过线则回改设计稿或参数重跑（最多 2 轮），再交付。

无视觉能力且无可用子模型时降级：`--mode icon`（强分面+形态学清洗，图案感弱于主路径）。

**无视觉能力的 agent 环境**：可用 `engine/design_emblem.py` 走 OpenAI 兼容视觉 API 自动出设计稿
（`OPENAI_API_KEY` 必填，`OPENAI_BASE_URL`/`OPENAI_MODEL`/`OPENAI_MAX_TOKENS` 可选，`--render` 直接出图），再按本工作流质检。

## 质检要求（必须）

出图后**不要直接交付**：按本目录 `PROMPTS.md` §三 的五大类质检清单（章体结构/主体纹章/文字/图层冲突/质感）逐项自检——
视觉能力自适应：自带图像识别则自行读图；否则委派任意可用视觉子模型（或其它可读图工具）。
发现问题按轮迭代后再交付。验收基准：糖果 ≥0.85 / 金属 ≥0.85 / 游戏关联 ≥0.7。

## 配套网页工具

同一算法的纯前端版「方舟蚀刻章设计器」位于本目录 `web/index.html`，支持拖拽上传、实时调参、AI 抠图模型（浏览器端加载，失败自动降级内置算法）。启动方式：

```powershell
python -m http.server 43985 --bind 127.0.0.1 --directory "<skill目录>/web"
```

访问 http://127.0.0.1:43985/ 。适合让用户自己精调参数；agent 侧批量出图走 CLI。

## 边界与限制

- 经典抠图对「主体清晰 + 背景较简洁」的照片效果最好；复杂背景请提示用户使用网页工具（浏览器端 AI 模型抠图）或勾选跳过抠图。
- 生成的图为二创风格演绎，仅供个人娱乐，勿用于商业用途。
